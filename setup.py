from setuptools import setup, find_packages

setup(
    name='bitin',
    version='0.1',
    packages=find_packages(),
    install_requires=[
    ],
    # Additional metadata
    author='Aditya',
    description='Digital Electronics Library',
    long_description=open('README.md').read(),
    long_description_content_type='text/markdown',
    url='https://github.com/hexronuspi/bitin/',
)
